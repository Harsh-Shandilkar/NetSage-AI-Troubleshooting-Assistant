import { CheckCircle2, Eye, Fingerprint, GitBranch, Hand, Lock, ShieldCheck, UserRound, XCircle } from 'lucide-react';
import { NetsageShell, PageIntro } from '@/components/netsage-shell';

const principles = [
  { icon: GitBranch, number: '01', title: 'Checks before opinion', body: 'Cisco evidence is evaluated by deterministic rules first. Every finding keeps its rule identifier, severity, and supporting signals attached.' },
  { icon: Eye, number: '02', title: 'Advisory, not autonomous', body: 'An optional AI diagnosis organizes the evidence into a likely cause and a safe next step. It does not have device credentials or a change channel.' },
  { icon: Hand, number: '03', title: 'A person makes the call', body: 'A network operator verifies the command output, accepts the recommendation, edits it, or rejects it. The decision is recorded with notes.' },
  { icon: Lock, number: '04', title: 'Evidence stays bounded', body: 'NetSage uses the symptom and output you provide for this troubleshooting workflow. Avoid pasting secrets, credentials, or unrelated customer data.' },
];

export default function ResponsibleAi() {
  return <NetsageShell><div className="mx-auto max-w-[1180px] px-5 py-8 sm:px-8 lg:px-12 lg:py-10"><PageIntro eyebrow="Responsible AI / operating contract" title="Useful enough to accelerate. Bounded enough to trust." description="NetSage is a review workspace for network troubleshooting—not an autopilot. This is the operating contract behind every recommendation." /><div className="grid gap-4 md:grid-cols-2">{principles.map(({ icon: Icon, number, title, body }) => <article key={number} className="rounded-xl border border-border bg-card p-5 shadow-sm sm:p-6"><div className="flex items-start justify-between"><div className="rounded-lg bg-primary/10 p-2.5 text-primary"><Icon className="h-5 w-5" /></div><span className="font-mono-app text-xs font-bold text-muted-foreground">{number}</span></div><h2 className="mt-7 text-lg font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{body}</p></article>)}</div><section className="mt-8 overflow-hidden rounded-2xl border border-sidebar-border bg-sidebar text-sidebar-foreground shadow-sm"><div className="grid gap-8 p-6 sm:p-8 lg:grid-cols-[.82fr_1.18fr] lg:p-10"><div><p className="eyebrow text-sidebar-primary">The review loop</p><h2 className="mt-3 max-w-sm font-display text-2xl font-bold tracking-tight">A recommendation is never a change request.</h2><p className="mt-4 max-w-sm text-sm leading-6 text-sidebar-foreground/65">The workflow deliberately ends at a human decision. That boundary is a product feature.</p></div><div className="grid gap-3 sm:grid-cols-3">{[['Evidence', 'Input is captured'], ['Analysis', 'Rules and model advise'], ['Approval', 'Operator decides']].map(([title, body], index) => <div key={title} className="relative rounded-xl border border-sidebar-border bg-sidebar-accent/50 p-4">{index < 2 && <div className="absolute -right-3 top-1/2 z-10 hidden h-px w-3 bg-sidebar-primary/60 sm:block" />}<span className="font-mono-app text-[10px] font-bold text-sidebar-primary">0{index + 1}</span><p className="mt-8 font-semibold">{title}</p><p className="mt-1 text-xs leading-5 text-sidebar-foreground/55">{body}</p></div>)}</div></div></section><section className="mt-8 grid gap-5 lg:grid-cols-2"><div className="rounded-xl border border-border bg-card p-6"><div className="flex items-center gap-3"><Fingerprint className="h-5 w-5 text-primary" /><h2 className="font-bold">What NetSage records</h2></div><ul className="mt-5 space-y-3 text-sm leading-6 text-muted-foreground">{['The network category, symptom, and supplied command output', 'Deterministic findings and evidence signals', 'The recommendation, confidence, OSI layer, risk, and next command', 'The human review status, edits, and notes'].map((item) => <li key={item} className="flex gap-3"><CheckCircle2 className="mt-1 h-4 w-4 shrink-0 text-primary" />{item}</li>)}</ul></div><div className="rounded-xl border border-border bg-card p-6"><div className="flex items-center gap-3"><ShieldCheck className="h-5 w-5 text-accent" /><h2 className="font-bold">What NetSage will not do</h2></div><ul className="mt-5 space-y-3 text-sm leading-6 text-muted-foreground">{['Log in to a switch, router, firewall, or controller', 'Execute a command or apply a configuration change', 'Treat confidence as certainty or replace operator judgment', 'Hide the evidence that led to a recommendation'].map((item) => <li key={item} className="flex gap-3"><XCircle className="mt-1 h-4 w-4 shrink-0 text-destructive" />{item}</li>)}</ul></div></section><div className="mt-8 flex items-center gap-3 rounded-lg border border-accent/25 bg-accent/10 px-4 py-3 text-xs leading-5 text-[hsl(32_72%_32%)]"><UserRound className="h-4 w-4 shrink-0" />For internship users: treat every result as a learning aid. Validate the command, understand the OSI layer, then ask a supervisor before changing production equipment.</div></div></NetsageShell>;
}
